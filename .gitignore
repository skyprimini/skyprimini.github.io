# Original camera files: large, HEIC (unsupported in Chrome/Firefox), and they
# still carry EXIF GPS coordinates. Only the stripped, resized copies in images/
# are committed and published.
pictures/

.DS_Store
