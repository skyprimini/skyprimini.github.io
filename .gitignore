# Camera originals stay local. They're huge, HEIC doesn't work in most
# browsers, and they still have GPS data in them. The web-ready copies
# in images/ are what actually gets published.
pictures/

.DS_Store
